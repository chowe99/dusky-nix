# Fallback monitor rule for hotplugged and external displays.
monitor=,preferred,auto,auto

# VRR is configured per-monitor as the 5th argument on the monitor= line.
# Example: monitor=eDP-1,preferred,auto,1,vrr,1
# Hyprland removed the old `misc:vfr` and `misc:vrr` global options.
